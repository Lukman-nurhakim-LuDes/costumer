[build]
  command = "npm install --force && npm run prisma:generate && npm run build"
  publish = ".next"
  # ...
```

**Tindakan Anda:**

1.  Pastikan Anda telah menambahkan `export const dynamic = 'force-dynamic';` ke semua file `/dashboard` Anda.
2.  Verifikasi bahwa **Build Command** Anda di Netlify saat ini adalah:
    ```bash
    npm install --force && npx prisma generate && npm run build